@model RAHSys.Apresentacao.Models.IndexViewModel
@{
    ViewBag.Title = "Gerenciar Perfil";
}

<h2>@ViewBag.Title.</h2>

<p class="text-success">@ViewBag.StatusMessage</p>
<div>
    <h4>Modifique suas configurações de perfil</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Senha:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Modificar senha", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Criar Senha", "SetPassword")
            }
            ]
        </dd>
    </dl>
</div>
